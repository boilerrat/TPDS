interface:
  display_name: "Code Reviewer"
  short_description: "Review code changes across key quality pillars"
  default_prompt: "Use $code-reviewer to analyze the current code changes and provide a structured review."
